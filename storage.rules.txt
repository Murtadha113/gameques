rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // صور الأسئلة المقترحة من اللاعبين - أي حد يقدر يرفع صورة، بس محد يقدر يشوفها أو يحذفها غير الأدمن
    match /pendingPhotos/{fileName} {
      allow write: if request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
      allow read, delete: if request.auth != null && request.auth.uid == 'WyzmPfdR9mNd6XKyc1jQzpMCb2x2';
    }
  }
}
